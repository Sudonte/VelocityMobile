<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/Widget.Velocity.Card.Glass"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="10dp"
    android:clickable="true"
    android:focusable="true"
    app:rippleColor="@color/velocity_red_subtle">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_glass_card_white"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="14dp">

        <com.google.android.material.card.MaterialCardView
            android:id="@+id/iconContainerPtx"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginEnd="12dp"
            app:cardCornerRadius="20dp"
            app:cardElevation="0dp"
            app:cardBackgroundColor="@color/velocity_green_soft">

            <ImageView
                android:id="@+id/ivPtxIcon"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_gravity="center"
                android:contentDescription="@null"
                android:src="@drawable/ic_check_circle"
                app:tint="@color/velocity_green_dark" />
        </com.google.android.material.card.MaterialCardView>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tvPtxTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:fontFamily="sans-serif-black"
                android:textColor="@color/black"
                android:textSize="14sp"
                tools:text="Payment Successful" />

            <TextView
                android:id="@+id/tvPtxSubtitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:ellipsize="end"
                android:maxLines="1"
                android:fontFamily="sans-serif-medium"
                android:textColor="@color/velocity_text_secondary"
                android:textSize="12sp"
                tools:text="Booking Payment  •  Executive Room" />

            <TextView
                android:id="@+id/tvPtxDate"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:fontFamily="sans-serif-medium"
                android:textColor="@color/velocity_inactive_gray"
                android:textSize="11sp"
                tools:text="Sep 02, 2026  •  10:24 AM" />

            <!-- Booking-level Paid/Remaining - authoritative payment_summary when the
                 backend has attached one, else the legacy fields (see
                 PaymentTransactionAdapter#bindPaymentProgress()). Never this one
                 transaction's own amount (tvPtxAmount, at right, already shows that). -->
            <TextView
                android:id="@+id/tvPtxPaymentProgress"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:fontFamily="sans-serif-medium"
                android:textColor="@color/velocity_text_secondary"
                android:textSize="11sp"
                tools:text="Paid ₱10,000.00  •  Remaining ₱0.00" />

            <!-- "N Receipt(s) Available" - only when booking.getReceipts() is
                 non-empty; tapping this whole card already opens
                 TransactionDetailsActivity, where every receipt is individually
                 listed (see item 11 of the Phase 5 spec - either UX is acceptable). -->
            <LinearLayout
                android:id="@+id/layoutPtxReceiptsAvailable"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:gravity="center_vertical"
                android:orientation="horizontal"
                android:visibility="gone"
                tools:visibility="visible">

                <ImageView
                    android:layout_width="12dp"
                    android:layout_height="12dp"
                    android:layout_marginEnd="4dp"
                    android:importantForAccessibility="no"
                    android:src="@drawable/ic_transaction_history"
                    app:tint="@color/velocity_red_primary" />

                <TextView
                    android:id="@+id/tvPtxReceiptsAvailable"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:fontFamily="sans-serif-black"
                    android:textColor="@color/velocity_red_primary"
                    android:textSize="10sp"
                    tools:text="2 Receipts Available" />
            </LinearLayout>
        </LinearLayout>

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="end"
            android:layout_marginStart="8dp">

            <TextView
                android:id="@+id/tvPtxAmount"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="sans-serif-black"
                android:textColor="@color/velocity_red_primary"
                android:textSize="14sp"
                tools:text="₱2,900.00" />

            <ImageView
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:layout_marginTop="4dp"
                android:layout_gravity="end"
                android:contentDescription="@null"
                android:src="@drawable/ic_arrow_forward"
                app:tint="@color/velocity_inactive_gray" />
        </LinearLayout>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
