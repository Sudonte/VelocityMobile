*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Release signing material must NEVER live in this repo - the real keystore
# and keystore.properties live outside it entirely (see app/build.gradle.kts'
# releaseKeystorePropertiesPath). These patterns are defense-in-depth only,
# in case either ever ends up here by mistake.
*.jks
*.keystore
keystore.properties

# Scratch/debug artifacts from ad-hoc development sessions - not part of the
# app itself, never intended for version control.
.check_counts.php
_patch_edit_rooms_amenities.php
landing_screenshot.png
profile_screen.png
_verify_screen2.png
