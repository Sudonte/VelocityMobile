# Committed fallback for the Secrets Gradle Plugin so the project still builds
# without a real key. Put your actual key in local.properties (gitignored) as
# MAPS_API_KEY=your_real_key instead of editing this file.
MAPS_API_KEY=DEFAULT_API_KEY
